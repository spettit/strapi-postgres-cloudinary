{
  "routes": [
    {
      "method": "GET",
      "path": "/participants",
      "handler": "participant.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/count",
      "handler": "participant.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/participants/:id",
      "handler": "participant.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/participants",
      "handler": "participant.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/participants/:id",
      "handler": "participant.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/participants/:id",
      "handler": "participant.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
